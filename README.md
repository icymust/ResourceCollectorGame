# ResourceCollectorGame
Многопользовательская браузерная игра-сборщик ресурсов для Kood Johvi

Игра представляет собой многопользовательскую онлайн-игру в реальном времени, где игроки соревнуются в сборе ресурсов (золото, серебро, бронза) на игровом поле. Поддерживает до нескольких игроков одновременно с синхронизацией через WebSocket.

## 🎮 Как играть

### Цель игры
Соберите как можно больше ресурсов и опередите других игроков!

### Управление
- **Стрелки клавиатуры** или **WASD** - движение по игровому полю
- **Мышь** - взаимодействие с интерфейсом лобби

### Типы ресурсов
- 🥇 **Золото** - самый ценный ресурс (больше всего очков)
- 🥈 **Серебро** - средняя ценность
- 🥉 **Бронза** - базовый ресурс

### Правила
1. Двигайтесь по игровому полю и собирайте ресурсы
2. Ресурсы появляются случайным образом на поле
3. Ваш счет обновляется в реальном времени
4. Следите за таблицей лидеров в правом верхнем углу
5. Игра продолжается пока есть подключенные игроки

### Интерфейс
- **Лобби** - настройка игрока и ожидание других игроков
- **Игровое поле** - основная область игры с сеткой 20x20
- **Таблица лидеров** - отображает счет всех игроков
- **Кнопка помощи (?)** - открывает инструкции прямо в игре

## Быстрый запуск

### Способ 1: Автоматический скрипт (рекомендуется)
```bash
# macOS/Linux
./start.sh

# Windows
start.bat

# Универсальный (любая ОС)
node start.js
```

### Способ 2: NPM команды
```bash
# Установка зависимостей (если нужно)
npm install

# Запуск сервера
npm start
# или
npm run dev
```

### Способ 3: Ручной запуск
```bash
# Установка зависимостей
npm install

# Запуск сервера
node src/server.js
```

## Установка

### Предварительные требования
- Node.js (рекомендуется версия 16 или выше)
- npm (обычно устанавливается с Node.js)

### Установка зависимостей
1. Склонируйте репозиторий:
```bash
git clone https://github.com/icymust/ResourceCollectorGame.git
cd ResourceCollectorGame
```

2. Установите зависимости из package.json:
```bash
npm install
```

Это установит следующие зависимости:
- `express` - веб-фреймворк для сервера
- `socket.io` - библиотека для работы с WebSocket соединениями

## Запуск

### Запуск сервера
Запустите сервер с помощью команды:
```bash
node src/server.js
```

После успешного запуска сервер будет доступен по адресу `http://localhost:3000` (или другому порту, указанному в коде сервера).

### Открытие игры
1. Убедитесь, что сервер запущен
2. Откройте веб-браузер
3. Перейдите по адресу `http://localhost:3000`
4. Начните играть!

## Удаленный доступ через ngrok

Для того чтобы друзья могли подключиться к вашей игре через интернет, используйте ngrok:

### Установка ngrok

**macOS (через Homebrew):**
```bash
brew install ngrok/ngrok/ngrok
```

**Или скачайте с официального сайта:**
1. Идите на https://ngrok.com/
2. Зарегистрируйтесь и скачайте ngrok
3. Распакуйте в удобную папку

### Настройка authtoken
```bash
ngrok config add-authtoken YOUR_AUTHTOKEN
```
*(Ваш authtoken можно найти в личном кабинете ngrok)*

### Запуск с ngrok

**Вариант 1: Отдельные терминалы**
```bash
# Терминал 1 - запуск сервера
npm start

# Терминал 2 - запуск ngrok
ngrok http 3000
```

**Вариант 2: Одной командой (если установлен concurrently)**
```bash
# Установите зависимости для разработки
npm install -D nodemon concurrently

# Запустите сервер и ngrok одновременно
npm run dev-tunnel
```

### Использование ngrok URL

После запуска ngrok вы увидите что-то вроде:
```
Session Status: online
Forwarding: https://abc123.ngrok.io -> http://localhost:3000
```

Теперь игроки могут подключаться по адресу `https://abc123.ngrok.io`

### Дополнительные команды NPM

Добавьте в ваш `package.json` следующие скрипты:
```json
{
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "tunnel": "ngrok http 3000",
    "dev-tunnel": "concurrently \"npm run dev\" \"ngrok http 3000 --log=stdout\"",
    "install-dev": "npm install -D nodemon concurrently"
  }
}
```

### Важные заметки о ngrok
- 🔄 Бесплатный ngrok меняет URL при каждом перезапуске
- ⏰ URL остается активным пока работает ngrok
- 💰 Для постоянного URL нужна платная подписка
- 🔒 Все соединения через ngrok зашифрованы (HTTPS)

### Проверка состояния сервера
Вы можете проверить статус сервера через:
- Локально: `http://localhost:3000/health`
- Через ngrok: `https://your-ngrok-url.ngrok.io/health`

## 📁 Структура проекта

### Frontend (Клиентская часть)
**`public/index.html`** - Главный HTML файл с разметкой игры
- Содержит структуру лобби, игрового поля, модальных окон
- Подключает все необходимые CSS и JavaScript файлы
- Включает help-модальное окно с инструкциями

### CSS стили (модульная архитектура)
**`public/css/base.css`** - Базовые стили и CSS переменные
- Глобальные цвета и шрифты
- Сброс стилей по умолчанию
- CSS переменные для консистентного дизайна

**`public/css/layout.css`** - Макет и позиционирование
- Основная сетка страницы
- Позиционирование игрового поля и интерфейса
- Адаптивные контейнеры

**`public/css/buttons.css`** - Стили всех кнопок
- Кнопка старта игры
- Кнопка готовности игрока
- Кнопка выхода из игры
- Hover эффекты и состояния

**`public/css/forms.css`** - Формы и поля ввода
- Стили для форм настройки игрока
- Поля ввода имени игрока
- Focus состояния и валидация

**`public/css/modals.css`** - Модальные окна
- Help модальное окно с инструкциями
- Стили для закрытия модальных окон
- Backdrop blur эффекты
- Предварительный просмотр ресурсов

**`public/css/game.css`** - Игровые элементы
- Стили игровых клеток (cells)
- Анимации ресурсов (золото, серебро, бронза)
- Эффекты сбора ресурсов
- Keyframe анимации

**`public/css/lobby.css`** - Интерфейс лобби
- Карточки игроков с glass-эффектом
- Кнопка помощи
- Host контроли
- Градиентные фоны

### JavaScript модули (ES6)
**`public/js/main.js`** - Главный модуль приложения
- Инициализация игры
- Координация между модулями
- Обработка состояний игры

**`public/js/state.js`** - Управление состоянием
- Хранение данных игроков
- Состояние игрового поля
- Синхронизация с сервером

**`public/js/net.js`** - Сетевое взаимодействие
- WebSocket соединение с сервером
- Отправка и получение данных
- Обработка подключений/отключений

**`public/js/input.js`** - Обработка пользовательского ввода
- Клавиатурное управление (стрелки, WASD)
- События мыши для интерфейса
- Предотвращение конфликтов ввода

**`public/js/render.js`** - Отрисовка игры
- Рендеринг игрового поля
- Отображение игроков и ресурсов
- Обновление UI в реальном времени

**`public/js/ui.js`** - Пользовательский интерфейс
- Управление лобби
- Модальные окна
- Таблица лидеров
- Уведомления

**`public/js/constants.js`** - Константы игры
- Размеры игрового поля
- Цвета и настройки
- Конфигурационные значения

**`public/js/fps.js`** - Счетчик FPS
- Мониторинг производительности
- Отображение FPS в реальном времени

### Backend (Серверная часть)
**`src/server.js`** - Главный файл сервера
- Express.js веб-сервер
- Socket.IO интеграция
- Статические файлы
- ngrok поддержка для удаленного доступа
- Graceful shutdown обработка

**`src/server/sockets.js`** - WebSocket обработка
- Управление подключениями игроков
- Обработка событий в реальном времени
- Синхронизация между клиентами

**`src/server/lobby.js`** - Логика лобби
- Управление списком игроков
- Готовность игроков
- Старт игры

**`src/server/game.js`** - Игровая логика
- Обработка движений игроков
- Генерация ресурсов
- Подсчет очков
- Проверка коллизий

**`src/server/state.js`** - Серверное состояние
- Хранение данных игроков на сервере
- Состояние игрового поля
- История игровых событий

**`src/server/movement.js`** - Система движения
- Валидация перемещений
- Границы игрового поля
- Предотвращение читерства

**`src/server/resources.js`** - Система ресурсов
- Генерация ресурсов на поле
- Логика сбора ресурсов
- Балансировка спавна

### Конфигурационные файлы
**`package.json`** - NPM конфигурация
- Зависимости проекта
- Скрипты запуска
- Метаданные проекта

**`start.js`** - Универсальный запуск
- Кроссплатформенный запуск сервера
- Автоматическое определение ОС

**`start.sh`** - Скрипт запуска для Unix/macOS
**`start.bat`** - Скрипт запуска для Windows

## ⚙️ Настройки и конфигурация

### Серверные настройки
В файле `src/server.js` можно настроить:
```javascript
const PORT = process.env.PORT || 3000; // Порт сервера
```

### Игровые константы
В файле `public/js/constants.js`:
```javascript
const GRID_SIZE = 20; // Размер игрового поля
const CELL_SIZE = 30; // Размер клетки в пикселях
```

### CSS переменные
В файле `public/css/base.css`:
```css
:root {
  --primary-color: #7289da;  /* Основной цвет */
  --text-color: #ffffff;     /* Цвет текста */
  --grid-size: 20;          /* Размер сетки */
}
```

### Типы ресурсов
Настраиваются в серверной логике:
- **Золото**: высокая ценность, редкий спавн
- **Серебро**: средняя ценность, обычный спавн  
- **Бронза**: низкая ценность, частый спавн

### Настройки ngrok
В `package.json` можно добавить дополнительные команды:
```json
{
  "scripts": {
    "tunnel-region": "ngrok http 3000 --region=eu",
    "tunnel-subdomain": "ngrok http 3000 --subdomain=mygame"
  }
}
```

## 🔧 Системные требования

### Для разработки
- Node.js 16+ (рекомендуется 18+)
- npm или yarn
- Современный браузер с поддержкой ES6 модулей
- (Опционально) ngrok для удаленного доступа

### Для игроков
- Любой современный браузер (Chrome, Firefox, Safari, Edge)
- Стабильное интернет соединение
- Клавиатура для управления

## 🚀 Продвинутые возможности

### Мониторинг производительности
Игра включает встроенный счетчик FPS для мониторинга производительности.

### Graceful Shutdown
Сервер корректно обрабатывает сигналы завершения и уведомляет всех игроков об отключении.

### Модульная архитектура
CSS и JavaScript разделены на логические модули для легкого обслуживания.

### Real-time синхронизация
Все игровые события синхронизируются в реальном времени между всеми подключенными игроками.

